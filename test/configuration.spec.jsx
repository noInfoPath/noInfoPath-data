//configuration.spec.js
describe("Testing noConfig", function(){
	var noConfig;

	beforeEach(function(){
		module("noinfopath.configuration");

		// inject(function($injector){
		// 	noManifest = $injector.get("noManifest");
		// })
	});

	it("should exist and be initialized.");
});